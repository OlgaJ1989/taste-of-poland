""" Includes configuration for the table_reservation app. """
from django.apps import AppConfig


class TableReservationConfig(AppConfig):
    """ Includes configuration for the table_reservation app. """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'table_reservation'
